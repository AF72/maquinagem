[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm --prefix backend install"]

[phases.build]
cmds = ["npm --prefix backend run db:generate"]

[start]
cmd = "npm --prefix backend start"
